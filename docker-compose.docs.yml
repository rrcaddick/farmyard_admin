services:
  docs:
    image: farmyard_admin_local_docs
    container_name: farmyard_admin_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./farmyard_admin:/app/farmyard_admin:z
    ports:
      - '9000:9000'
    command: /start-docs
